/** URL de consultation du white paper R4V3 (remplaçable par une URL externe). */
export const R4V3_WHITEPAPER_URL = '/r4v3-whitepaper.txt';

/** Fichier texte téléchargeable représentant le white paper. */
export const R4V3_WHITEPAPER_TXT_URL = '/r4v3-whitepaper.txt';

export const R4V3_WHITEPAPER_TXT_FILENAME = 'R4V3-whitepaper.txt';

/** Nombre d'événements visibles dans le fil sans scroll. */
export const R4V3_ACTIVITY_PREVIEW = 3;

/** Parité officielle affichée dans l'application. */
export const R4V3_PEG_LABEL = '1 R4V3 = 1 CHF / 1 GBP';

/** Libellé long pour contenus pédagogiques. */
export const R4V3_PEG_LABEL_LONG = '1 R4V3 = 1 franc suisse ou 1 livre sterling';

/** Libellé court (franc suisse). */
export const R4V3_PEG_LABEL_SHORT = '1 R4V3 = 1 franc suisse';

/** Paire de cotation native R4V3. */
export const R4V3_PAIR_LABEL = 'R4V3 / CHF';

/** Identifiant paire chart / API. */
export const R4V3_CHART_PAIR = 'R4V3-CHF';

/** Valeur peg affichée dans le graphique hub. */
export const R4V3_PEG_DISPLAY_PRICE = '1,00 CHF';

/** Variation peg affichée (R4V3 indexé 1:1 sur CHF). */
export const R4V3_PEG_DISPLAY_DELTA = '0,00 %';
