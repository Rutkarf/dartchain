# Cloudflare Pages — build via scripts/cloudflare-build.sh, deploy via scripts/cloudflare-deploy.sh
# Utiliser `wrangler pages deploy` (PAS `wrangler deploy`).

name = "dartchain"
compatibility_date = "2025-07-16"
pages_build_output_dir = "apps/dartchain-frontend/Dart/dist/browser"
